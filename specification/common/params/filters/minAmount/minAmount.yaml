name: minAmount
in: query
description: |
  Filter results with `amount` not less than specified Integer value of 1/10^8 tokens (1 ADM = 100000000).
schema:
  type: integer
  example: 1000000000000001
required: false
