allOf:
  - $ref: "./BaseTransaction.yaml"
  - type: object
    required:
      - asset
      - type
    properties:
      type:
        type: integer
        minimum: 8
        maximum: 8
        example: 8
        description: Always equal to `8`
      asset:
        $ref: "./assets/ChatMessageAsset.yaml"
