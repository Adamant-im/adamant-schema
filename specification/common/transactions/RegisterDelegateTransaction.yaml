allOf:
  - $ref: "./BaseTransaction.yaml"
  - type: object
    required:
      - type
      - asset
    properties:
      type:
        type: integer
        minimum: 2
        maximum: 2
        example: 2
        description: Always equal to `2`
      asset:
        $ref: "./assets/RegisterDelegateAsset.yaml"
