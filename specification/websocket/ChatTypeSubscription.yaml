description: One asset.chat.type value or a list of values to add to the socket subscription.
oneOf:
  - type: integer
    minimum: 0
    maximum: 3
  - type: array
    minItems: 1
    uniqueItems: true
    items:
      type: integer
      minimum: 0
      maximum: 3
example:
  - 1
  - 2
  - 3
